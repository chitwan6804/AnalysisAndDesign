#include <iostream>
#include <string>
using namespace std;

void LCS(string first,string second){
    int m=sizeof(first);
    int n=sizeof(second);
    for(int i=0;i<=m;i++){

    }
}

int main(){
    string first;
    string second;
    cout<<"Enter first string: ";
    cin>>first;
    cout<<"Enter Second String: ";
    cin>>second;
    LCS(first,second);
}